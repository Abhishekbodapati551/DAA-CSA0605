def is_palindrome(s):
        return True
    if s[0] == s[-1]:
        return is_palindrome(s[1:-1])
    else:
        return False
s = "racecar"
if is_palindrome(s):
    print(f'"{s}" is a palindrome')
else:
    print(f'"{s}"
